/**
 * Enum containing all valid discount codes
 * Used to avoid duplication and ensure consistency across tests
 */
export enum DiscountCode {
  SAVE10 = 'SAVE10',
  SAVE20 = 'SAVE20',
  HALF = 'HALF',
}
